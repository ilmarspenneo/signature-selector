import React from 'react';
import ReactDOM from 'react-dom';

// Your main component (e.g., App) import statement goes here
import App from './App'; // Replace with your actual component import

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
